#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'data' => [],
        'tests' => [qw(
            pickDeliverAdd
            )],

        'documentation' => [qw(
            pickDeliverAdd
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
